import { MouseMoveDirective } from './mouse.move.directive';

describe('MouseMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseMoveDirective();
    expect(directive).toBeTruthy();
  });
});
